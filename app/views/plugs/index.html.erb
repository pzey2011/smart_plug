<p id="notice"><%= notice %></p>

<h1>Listing Plugs</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @plugs.each do |plug| %>
      <tr>
        <td><%= link_to 'Show', plug %></td>
        <td><%= link_to 'Edit', edit_plug_path(plug) %></td>
        <td><%= link_to 'Destroy', plug, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Plug', new_plug_path %>
